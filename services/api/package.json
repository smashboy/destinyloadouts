{
  "name": "@services/api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dotenv -e .env.local -- tsx watch server",
    "db:docker": "docker compose up -d",
    "db:migrate-dev": "dotenv -e .env.local -- npx prisma migrate dev",
    "db:manifest": "dotenv -e .env.local -- tsx ./prisma/fetchDestinyManifestTables.ts",
    "db:reset": "dotenv -e .env.local -- npx prisma migrate reset --force --preview-feature",
    "prisma:generate": "npx prisma generate",
    "prisma:studio": "dotenv -e .env.local -- npx prisma studio"
  },
  "dependencies": {
    "@fastify/cors": "^8.2.1",
    "@prisma/client": "^4.11.0",
    "@trpc/server": "^10.17.0",
    "fastify": "^4.15.0"
  },
  "devDependencies": {
    "@types/node": "18.15.0",
    "dotenv": "^16.0.3",
    "next-auth": "^4.20.1",
    "prisma": "^4.11.0",
    "tailwindcss": "^3.2.7",
    "tsx": "^3.12.5",
    "typescript": "4.9.5"
  }
}
